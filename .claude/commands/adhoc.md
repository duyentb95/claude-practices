Handle an ad-hoc request outside current sprint.

1. Create isolated task in `.bmad/adhoc/ADHOC_NNN_short_name.md`
2. Execute the request (GSD mode if < 15 min, delegate if complex)
3. After completion, extract reusable knowledge into `.bmad/knowledge/`
4. Do NOT modify `.bmad/MASTER_PLAN.md` — this is out-of-sprint work

Request: $ARGUMENTS
